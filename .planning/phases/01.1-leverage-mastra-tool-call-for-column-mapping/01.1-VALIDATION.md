---
phase: 01.1
slug: leverage-mastra-tool-call-for-column-mapping
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-15
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest |
| **Config file** | `apps/web/vitest.config.ts` |
| **Quick run command** | `cd apps/web && pnpm vitest run src/mastra/tools/map-schema-tool.test.ts` |
| **Full suite command** | `cd apps/web && pnpm test:once` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd apps/web && pnpm vitest run src/mastra/tools/map-schema-tool.test.ts`
- **After every plan wave:** Run `cd apps/web && pnpm test:once`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 0 | Wave 0 setup | unit | `cd apps/web && pnpm vitest run src/mastra/tools/map-schema-tool.test.ts` | ❌ W0 | ⬜ pending |
| 01.1-01-02 | 01 | 1 | LLM mapping | unit (mocked) | `cd apps/web && pnpm vitest run src/mastra/tools/map-schema-tool.test.ts` | ❌ W0 | ⬜ pending |
| 01.1-01-03 | 01 | 1 | Post-processing | unit | `cd apps/web && pnpm vitest run src/mastra/tools/map-schema-tool.test.ts` | ❌ W0 | ⬜ pending |
| 01.1-01-04 | 01 | 1 | Error handling | unit (mocked) | `cd apps/web && pnpm vitest run src/mastra/tools/map-schema-tool.test.ts` | ❌ W0 | ⬜ pending |
| 01.1-01-05 | 01 | 1 | Cleanup old code | manual | N/A — verify deleted code | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `apps/web/src/mastra/tools/map-schema-tool.test.ts` — unit tests for LLM-powered mapping
- [ ] Mock for `generateObject` — `vi.mock("ai")` to avoid real API calls in tests
- [ ] Test fixtures: sample headers, sample rows, expected mapping outputs

*Existing infrastructure (Vitest) covers framework needs. Only test file creation required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Deterministic code fully deleted | Cleanup | Verify absence of HEADER_SYNONYMS, substring matching | `grep -r "HEADER_SYNONYMS" apps/web/src/` returns nothing |
| API route unchanged | Interface stability | Integration test would need full server | Compare `route.ts` git diff — should have no changes |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
