---
phase: 01.1-leverage-mastra-tool-call-for-column-mapping
plan: 02
subsystem: ai
tags: [llm-failure-signaling, csv-import, api-contract, mastra, column-mapping]

# Dependency graph
requires:
  - phase: 01.1-leverage-mastra-tool-call-for-column-mapping
    plan: 01
    provides: LLM-powered mapSchemaTool with generateObject, 7 unit tests
provides:
  - llmFailed boolean flag in mapSchemaTool output for error signaling
  - API route propagation of llmFailed to frontend
  - csv-import-sheet uses correct headers/sampleRows API contract
  - Warning toast on LLM failure for manual mapping fallback
affects: [csv-import, analyze-csv-route, csv-import-sheet]

# Tech tracking
tech-stack:
  added: []
  patterns: [LLM error signaling via boolean flag in tool output, parseCsvData reuse across import flows]

key-files:
  created: []
  modified:
    - apps/web/src/mastra/tools/map-schema-tool.ts
    - apps/web/src/mastra/tools/map-schema-tool.test.ts
    - apps/web/src/app/api/analyze-csv/route.ts
    - apps/web/src/app/(workspace)/clients/components/csv-import-sheet.tsx

key-decisions:
  - "Used boolean llmFailed flag (not error codes) for simple binary signal of LLM health"
  - "Reused parseCsvData in csv-import-sheet.tsx instead of inline PapaParse for BOM stripping and dynamicTyping: false"

patterns-established:
  - "LLM failure signaling: Tool returns llmFailed boolean alongside empty fallback data, letting UI distinguish failure from genuine empty results"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-03-15
---

# Phase 01.1 Plan 02: LLM Failure Signaling and API Contract Fix Summary

**llmFailed flag in mapSchemaTool + API route, csv-import-sheet fixed to send headers/sampleRows with LLM failure warning toast**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-15T12:47:27Z
- **Completed:** 2026-03-15T12:50:35Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added llmFailed boolean to mapSchemaTool outputSchema, success path (false), and error path (true)
- API route propagates llmFailed to frontend response JSON
- Fixed csv-import-sheet.tsx to send {headers, sampleRows, entityType} instead of {csvContent, entityType}
- Replaced inline PapaParse call with parseCsvData utility (fixes dynamicTyping: true bug, gets BOM stripping)
- Added warning toast when LLM mapping fails so users know to map columns manually
- Increased LLM timeout from 15s to 30s to reduce false timeouts on cold starts
- All 9 unit tests pass (7 existing + 2 new, with 2 updated assertions)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add llmFailed flag to mapSchemaTool and propagate through API route** - `9d5f7dc` (feat - TDD)
2. **Task 2: Fix csv-import-sheet.tsx to use correct API contract** - `31ae968` (fix)

## Files Created/Modified
- `apps/web/src/mastra/tools/map-schema-tool.ts` - Added llmFailed to outputSchema and both return paths, increased timeout to 30s
- `apps/web/src/mastra/tools/map-schema-tool.test.ts` - Added Tests 8+9 for llmFailed, updated Tests 4+5 assertions
- `apps/web/src/app/api/analyze-csv/route.ts` - Propagates llmFailed from mapResult to response JSON
- `apps/web/src/app/(workspace)/clients/components/csv-import-sheet.tsx` - Fixed API contract (headers/sampleRows), parseCsvData reuse, LLM failure toast

## Decisions Made
- Used boolean llmFailed flag (not error codes) for simple binary signal -- the UI only needs to know "did it work or not"
- Reused parseCsvData from transform-csv.ts instead of duplicating PapaParse config -- DRY and gets BOM stripping for free

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- TypeScript error on `analysisResult as Record<string, unknown>` -- CsvAnalysisResult interface doesn't have index signature. Fixed with double assertion via `unknown`.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Both CSV import flows (wizard and sheet) now send correct API format
- LLM failures are visible to users with actionable guidance
- All unit tests pass, TypeScript compiles cleanly

## Self-Check: PASSED

All files exist and all commits verified.

---
*Phase: 01.1-leverage-mastra-tool-call-for-column-mapping*
*Completed: 2026-03-15*
