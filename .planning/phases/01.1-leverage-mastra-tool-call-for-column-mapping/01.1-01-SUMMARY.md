---
phase: 01.1-leverage-mastra-tool-call-for-column-mapping
plan: 01
subsystem: ai
tags: [generateObject, ai-sdk, openai, gpt-5-nano, mastra, csv-import, column-mapping]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: mapSchemaTool with deterministic matching, CLIENT_SCHEMA_FIELDS, analyze-csv route
provides:
  - LLM-powered column mapping via generateObject with GPT-5 nano
  - Post-processing pipeline for field validation and duplicate resolution
  - Graceful degradation on LLM failure (empty mappings, user maps manually)
  - 7 unit tests with mocked generateObject
affects: [csv-import, analyze-csv-route]

# Tech tracking
tech-stack:
  added: []
  patterns: [generateObject inside Mastra tool execute, LLM structured output with Zod, post-processing for LLM response validation]

key-files:
  created:
    - apps/web/src/mastra/tools/map-schema-tool.test.ts
  modified:
    - apps/web/src/mastra/tools/map-schema-tool.ts
    - apps/web/src/app/api/analyze-csv/route.ts
    - apps/web/vitest.config.ts

key-decisions:
  - "Used generateObject from ai SDK directly (not Mastra agent.generate) for simpler single-call structured extraction"
  - "Used z.nullable() instead of z.optional() in LLM response schema for OpenAI structured output compatibility"

patterns-established:
  - "generateObject inside tool execute: Call AI SDK generateObject directly from Mastra tool execute function for structured LLM extraction"
  - "LLM post-processing: Validate LLM field names against schema, resolve duplicates by confidence, enrich with schema metadata"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-03-15
---

# Phase 01.1 Plan 01: LLM Column Mapping Summary

**LLM-powered CSV column mapping via AI SDK generateObject with GPT-5 nano, replacing all deterministic synonym/substring matching**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-15T12:06:38Z
- **Completed:** 2026-03-15T12:09:26Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Replaced entire deterministic matching engine (HEADER_SYNONYMS map, substring logic) with single generateObject call to GPT-5 nano
- Added post-processing pipeline that validates schema field names, resolves duplicate assignments by confidence, and enriches with dataType/isRequired
- Graceful fallback on any LLM failure returns empty mappings with all columns unmapped for manual user mapping
- 7 unit tests covering valid mappings, invalid fields, duplicates, API errors, timeouts, prompt content, and missing required fields

## Task Commits

Each task was committed atomically:

1. **Task 1: Write test scaffold with mocked generateObject** - `9b411b7` (test - TDD RED)
2. **Task 2: Rewrite mapSchemaTool internals with generateObject** - `f0b09a7` (feat - TDD GREEN)

## Files Created/Modified
- `apps/web/src/mastra/tools/map-schema-tool.ts` - Rewritten to use generateObject with GPT-5 nano, buildMappingPrompt, and postProcessMappings
- `apps/web/src/mastra/tools/map-schema-tool.test.ts` - 7 unit tests with mocked generateObject
- `apps/web/src/app/api/analyze-csv/route.ts` - Comment update only (deterministic -> LLM)
- `apps/web/vitest.config.ts` - Added path alias resolution for @/ imports

## Decisions Made
- Used generateObject from ai SDK directly instead of Mastra agent.generate() -- simpler for single structured extraction call, avoids agent overhead
- Used z.nullable() for sampleValue in LLM response schema per OpenAI structured output compatibility requirement
- Prompt explicitly explains dot-namespace convention to prevent LLM from inventing nested field paths

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added path alias resolution to vitest config**
- **Found during:** Task 1 (test scaffold creation)
- **Issue:** Vitest could not resolve @/ path alias used in map-schema-tool.ts imports
- **Fix:** Added resolve.alias config mapping @ to src directory in vitest.config.ts
- **Files modified:** apps/web/vitest.config.ts
- **Verification:** All 7 tests run successfully with alias resolution
- **Committed in:** 9b411b7 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Essential fix for test infrastructure. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required. Uses existing OPENAI_API_KEY environment variable.

## Next Phase Readiness
- mapSchemaTool now produces real LLM confidence scores for the column mapping UI
- API route interface unchanged -- no downstream changes needed
- All deterministic matching code deleted -- clean codebase

## Self-Check: PASSED

All files exist and all commits verified.

---
*Phase: 01.1-leverage-mastra-tool-call-for-column-mapping*
*Completed: 2026-03-15*
