// Filled by Plan 14-03.
//
// Wave 0 todo-skeleton: tests are declared as `it.todo` so the suite passes
// (todo = skipped) and the file is registered with vitest. Plan 14-03 fills
// the bodies with React Testing Library assertions.

import { describe, it } from "vitest";

describe("SignatureCard", () => {
	it.todo("renders Type tab by default with Caveat live preview");
	it.todo("toggle to Draw mode lazy-loads SignatureCanvasPad");
	it.todo("typed mode requires name.trim().length >= 2");
	it.todo("emits non-usable payload when disabled prop is true");
});
