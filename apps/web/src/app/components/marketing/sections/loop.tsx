import { Eyebrow, Section, SectionHeading } from "../primitives";

/** WAVE-1 STUB — implement per .planning/landing-redesign-2026-08/DIRECTION.md. */
export function Loop() {
	return (
		<Section id="loop">
			<Eyebrow>The new way</Eyebrow>
			<SectionHeading>One loop. From the first call to the money landing.</SectionHeading>
		</Section>
	);
}
