import { Eyebrow, Section, SectionHeading } from "../primitives";

/** WAVE-1 STUB — implement per .planning/landing-redesign-2026-08/DIRECTION.md. */
export function OnePlace() {
	return (
		<Section id="one-place">
			<Eyebrow>The old way</Eyebrow>
			<SectionHeading>Five places to look. One job to do.</SectionHeading>
		</Section>
	);
}
