import { Eyebrow, Section, SectionHeading } from "../primitives";

/** WAVE-1 STUB — implement per .planning/landing-redesign-2026-08/DIRECTION.md. */
export function Switching() {
	return (
		<Section id="switching">
			<Eyebrow>Switching over</Eyebrow>
			<SectionHeading>Bring the list you already have.</SectionHeading>
		</Section>
	);
}
