import { Eyebrow, Section, SectionHeading } from "../primitives";

/** WAVE-1 STUB — implement per .planning/landing-redesign-2026-08/DIRECTION.md. */
export function MoneyTime() {
	return (
		<Section id="money-time">
			<Eyebrow>Money &amp; time</Eyebrow>
			<SectionHeading>Two things it buys back.</SectionHeading>
		</Section>
	);
}
