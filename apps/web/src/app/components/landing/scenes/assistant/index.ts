/**
 * A-101 act 3 — "Ask your business anything".
 *
 * The rail imports `assistantMeta` for its sheet tab and copy, and
 * `AssistantScene` for the stage. Nothing else in this folder is public.
 */

export { assistantMeta, type SceneMeta } from "./meta";
export { AssistantScene } from "./scene";
