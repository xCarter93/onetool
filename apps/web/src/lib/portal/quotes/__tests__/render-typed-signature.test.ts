// Filled by Plan 14-03.
//
// Wave 0 todo-skeleton: tests are declared as `it.todo` so the suite passes
// (todo = skipped) and the file is registered with vitest. Plan 14-03 fills
// the bodies.

import { describe, it } from "vitest";

describe("renderTypedSignature", () => {
	it.todo("awaits document.fonts.load + document.fonts.ready before fillText");
	it.todo("scales canvas backing-store by devicePixelRatio");
	it.todo("returns dataUrl matching /^data:image\\/png;base64,/");
	it.todo("returns raw object with typedName and font='Caveat'");
});
